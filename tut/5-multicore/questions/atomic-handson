/* thread 1 /
mov x0, #0
mov x1, addr of counter
loop:
    
    ldadda x1, x1, [x1]
   
    ldadda x0, x0, [x0]
    cmp x0, #1000
    ble loop

/ program 2 */
mov x0, #0
mov x1, addr of counter
loop:
    
    ldadda x1, x1, [x1]
   
    ldadda x0, x0, [x0]
    cmp x0, #1000
    ble loop